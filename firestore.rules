rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // POS Orders — Allow read/write from authenticated clients
    // TODO: Add proper authentication rules before production
    match /pos_orders/{orderId} {
      allow read, write: if true; // ⚠️ DEV ONLY — lock down before production
    }
  }
}
